services:
  app:
    build: .
    container_name: clinic_api
    env_file:
      - .env
    ports:
      - "0.0.0.0:8000:8000"
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"
